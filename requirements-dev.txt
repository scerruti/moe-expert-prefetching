# Development dependencies (tests, linting, formatting).
#
# Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0

# --- Linting / formatting / typing ---
black>=24.3.0
ruff>=0.4.0
mypy>=1.9.0

# --- Interactive exploration ---
ipython>=8.20.0
